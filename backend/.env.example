# MemeVault Backend - BLUEPRINT uyumlu
# Bu dosyayı .env olarak kopyalayıp doldurun.

# Veritabanı (docker-compose postgres)
DATABASE_URL=postgresql+asyncpg://memevault:memevault@localhost:5432/memevault
DATABASE_URL_SYNC=postgresql+psycopg2://memevault:memevault@localhost:5432/memevault

# JWT (production'da güçlü key)
SECRET_KEY=change-me-in-production

# Redis / Celery
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1

# MinIO (docker-compose minio)
S3_ENDPOINT_URL=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=memevault

# OpenAI (caption + embedding)
OPENAI_API_KEY=sk-...
